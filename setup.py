from setuptools import setup, find_packages

setup(
    name="eos_line_search",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # add can dependencies I need here in the future
    ],
)
